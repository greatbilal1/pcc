keywords = {
    "variable": "A variable is a container that holds some value",
    "constant": "It's a special type of variable whose value can't be changed.",
    "is": "It a keywords which check if both the conditions points to same location",
    "in": "It's a keyword which check if a condition(variable) is located in a list or tuple",
    "list": "It's a list",
}

print(keywords.get("variable"))
print(keywords.get("constant"))
print(keywords.get("is"))
print(keywords.get("in"))
print(keywords.get("list"))
